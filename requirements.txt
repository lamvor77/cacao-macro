customtkinter>=5.2.0
pywin32>=306
psutil>=5.9.0
pywinauto>=0.6.8
pyautogui>=0.9.54
pyperclip>=1.8.2
schedule>=1.2.0
Pillow>=10.0.0

# ===== Phase 2: 클라우드 동기화 (선택 기능) =====
# SUPABASE_ENABLED=False(기본값)이면 아래 패키지가 없어도 프로그램은
# 정상 실행된다 (services/supabase_client.py가 ImportError를 안전하게 처리함).
#
# supabase 하위의 realtime/postgrest/supabase-auth/pydantic/websockets는
# 전부 이번에 실제로 검증한 버전으로 정확히 고정한다(== 고정, >= 아님) —
# 이전에는 supabase>=2.0.0만 있고 하위 패키지는 전이 의존성으로 버전이
# 고정돼 있지 않아, 설치 시점에 따라 조용히 다른 버전이 섞여 들어올 수
# 있었다. 특히 services/realtime_protocol_compat.py는 realtime==2.31.0의
# _listen() 내부 구조를 그대로 복제한 호환 계층이라, 이 버전이 벗어나면
# 호환 계층 자체가 조용히 안 맞을 수 있다(안전 실패로 설계돼 있지만,
# 애초에 버전이 고정돼 있는 편이 안전하다).
supabase==2.31.0
realtime==2.31.0
postgrest==2.31.0
supabase-auth==2.31.0
pydantic==2.13.4
websockets==15.0.1
python-dotenv>=1.0.0

# ===== Release Candidate Sprint 1: 버전 비교 =====
# 프리릴리스 버전 순서(1.0.0-rc.1 < 1.0.0 < 1.0.1)를 올바르게 비교하기 위해
# 사용한다(core/version_check.py). 순수 Python 패키지라 PyInstaller가 별도
# 설정 없이 포함한다.
packaging>=23.0
